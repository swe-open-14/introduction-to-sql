/*
 Create the `people` table:

 +----+------------+-----------+-------+
 | id | first_name | last_name | email |
 +----+------------+-----------+-------+

 Write an SQL statement to create the `people` table. The `id` field (an
 integer) is the primary key. The `first_name` and `last_name` fields should
 be able to store a string up to 255 characters long and are required. The
 `email` field should also be able to store a string up to 255 characters
 long, but is optional.
 */
